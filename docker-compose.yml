version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:5000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
